<%= simple_form_for [ doctor, message ], remote: true do |f| %>
    <div class="d-flex flex-column ">
        <%= f.input :content, label: false, placeholder: "Message" , as: :text %>
            <%= f.button :submit, "Envoyer", class: "button accept" %>
    </div>
    <% end %>